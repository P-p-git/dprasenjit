# JWT Secret key for authentication (change this to a random string in production)
JWT_SECRET=your_jwt_secret_key_here

# Server port
PORT=5000

# Environment: development | production
NODE_ENV=development

# Optional: custom directory for the SQLite database file (used for Render persistent disks)
# DATA_DIR=/var/data
